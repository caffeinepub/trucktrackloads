/**
 * Shared constants for admin token management
 * Ensures consistent event names and storage keys across the application
 */

export const ADMIN_TOKEN_KEY = 'caffeineAdminToken';
export const ADMIN_TOKEN_CHANGE_EVENT = 'caffeineAdminTokenChange';
